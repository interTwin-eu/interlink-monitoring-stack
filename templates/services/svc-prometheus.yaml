apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.prometheus.name }}
  namespace: {{ .Release.Namespace }}
spec:
  ports:
    - name: {{ .Values.services.prometheus.name }}
      port: {{ .Values.services.prometheus.port }}
      protocol: TCP
      targetPort: {{ .Values.services.prometheus.port }}
  selector:
    app: {{ .Values.app.name }}
  type: ClusterIP
status:
  loadBalancer: {}
